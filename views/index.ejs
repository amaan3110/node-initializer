<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Node - Initializer</title>
    <link
      rel="icon"
      type="image/png"
      href="https://img.icons8.com/color/64/nodejs.png"
    />

    <!-- BOOTSTRAP CDN -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
    />

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/lodash@4.17.21/lodash.min.js"></script>

    <style>
      @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap");

      * {
        box-sizing: border-box;
      }

      body {
        font-family: "Poppins", sans-serif;
      }
      main {
        max-width: 1600px;
        width: 100%;
        margin: 0 auto;
      }
      input:focus,
      button:focus,
      textarea:focus {
        outline: none;
      }

      .metaData label {
        min-width: 100px;
        text-align: right;
      }

      .metaData input {
        font-size: 15px;

        &:focus {
          border-bottom: 2px solid #157347 !important;
        }
      }

      ul {
        font-size: 15px;
      }

      .dependency-item {
        cursor: pointer;
        transition: background-color 0.3s ease;
      }
      .dependency-item:hover {
        background-color: #f8f9fa;
        border-radius: 5px;
      }

      .small {
        font-size: 13px !important;
      }
    </style>
  </head>
  <body>
    <main class="container my-5">
      <h1 class="text-center">Node - Initializer</h1>
      <div class="border border-1 p-4 pb-0 mt-3 rounded-2">
        <div class="row gap-4 gap-md-0">
          <div class="col-md-6">
            <div id="projectLanguage">
              <h6 class="fw-semibold">Language</h6>
              <label for="js">JavaScript</label>
              <input type="radio" name="language" id="js" checked />
              <label class="ms-2" for="ts">TypeScript</label>
              <input type="radio" name="language" id="ts" />
              <p
                class="small text-muted mt-1 text-decoration-underline"
                id="noteForTS"
              ></p>
            </div>
            <div class="mt-4 metaData">
              <h6 class="fw-semibold">Project Metadata</h6>
              <div class="mt-3">
                <label class="me-2" for="entryPoint">Entry Point:</label>
                <input
                  type="text"
                  id="entryPoint"
                  class="w-50 px-2 py-1 border-0 border-bottom border-2"
                  value="index.js"
                />
              </div>
              <div class="mt-3">
                <label class="me-2" for="port">Port:</label>
                <input
                  type="text"
                  id="port"
                  class="w-50 px-2 py-1 border-0 border-bottom border-2"
                  value="3000"
                />
              </div>
              <div class="mt-3">
                <label class="me-2" for="description">Description:</label>
                <input
                  type="text"
                  id="description"
                  class="w-50 px-2 py-1 border-0 border-bottom border-2"
                  placeholder="Demo project for Node.js"
                  value="Demo project for Node.js"
                />
              </div>
            </div>
            <div class="mt-4">
              <h6 class="fw-semibold">Folder Structure</h6>
              <label for="basic">Basic</label>
              <input type="radio" name="folderStructure" id="basic" checked />
              <label class="ms-3" for="mvc">MVC</label>
              <input type="radio" name="folderStructure" id="mvc" />
              <label class="ms-3" for="mvc">Modular</label>
              <input type="radio" name="folderStructure" id="modular" />
            </div>
          </div>
          <div class="col-md-6">
            <h6 class="fw-semibold">Dependencies</h6>
            <p class="small text-muted mb-1 text-decoration-underline">
              Add your dependencies carefully — they will be installed
              automatically and included in your project 🚀
            </p>
            <input
              class="w-100 p-2 rounded-2 border border-1"
              style="font-size: 14px"
              type="text"
              placeholder="express, mongoose, cors"
              id="dependency"
            />
            <div class="mt-3">
              <ul id="dependencyList" class="list-unstyled"></ul>
            </div>
            <div class="mt-3" id="dependencyContainer"></div>
          </div>
        </div>
        <div class="d-flex align-items-center justify-content-center mt-4 p-3">
          <button
            type="button"
            class="btn btn-success w-50 shadow-sm"
            id="generateFile"
          >
            <i class="bi bi-rocket-takeoff-fill me-1"></i>
            Generate
          </button>
        </div>
      </div>
    </main>
    <script src="/script.js"></script>
  </body>
</html>
